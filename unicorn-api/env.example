# Environment
ENVIRONMENT=development

# Server
PORT=8080

# Logging
LOG_LEVEL=info

# Database (for future use)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=unicorn_api
DB_USER=postgres
DB_PASSWORD=password

# JWT (for future use)
JWT_SECRET=your-secret-key
JWT_EXPIRY=24h 